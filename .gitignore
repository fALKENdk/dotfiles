# macOS / editor noise
.DS_Store
*.swp
*.swo
*.tmp
*.bak

# Logs and shell/runtime artifacts
**/*.log
**/*_history
**/.zcompdump*
**/.python_history
**/.lesshst

# Machine-specific local config (backed up via dotfiles-local)
local/

# AI agent skills: track .skill-lock.json (and skills-custom/), not the
# source-installed skills, which are rehydrated via `npx skills install`.
ai/agents/skills/

# Encrypted backups (managed by dotfiles backup/restore)
backups/

# Secrets and credentials (never commit)
**/.ssh/id_*
**/.ssh/*_key
**/*.pem
**/*.p12
**/*.pfx
**/*.kdbx
**/*credentials*
**/*.token
**/.env
**/.env.*
!**/.env.example

